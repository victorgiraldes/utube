<nav class= "indigo darken-4">
  <div class="nav-wrapper">
      <ul id="nav-mobile" class="right hide-on-med-and-down">
        <% if user_signed_in? %>
        <%= link_to "#{@user.name}", profile_path(current_user.id) %>
        <% end %>
      </ul>
  </div>
</nav>
